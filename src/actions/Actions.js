import actionTypes from './ActionTypes.js';
import dispatcher from '../Dispatcher.js';

const Actions = {
    searchPlayer(part) {
        //TODO(implement);
    },

    loadPlayer(id) {
        //TODO(implement);
    },

    loadGeneral() {
        //TODO(implement);
    }
}
